<h1><%= @location.name %></h1>
<%= link_to "HOME", root_url %>
<%= link_to "EDIT", edit_location_path(@location) %>

<%= link_to 'DELETE', @location, method: :destroy, data: {confirm: 'Are you sure you want to delete this location?'} %>


<p>
	<%= @location.review %>
</p>
<img src="<%= @location.picture_url %>">
<br>
